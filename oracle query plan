EXPLAIN PLAN SET STATEMENT_ID = 'my_query' INTO PLAN_TABLE FOR
SELECT *
FROM your_table
ORDER BY created_date DESC
FETCH FIRST 30 ROWS ONLY;




SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY('PLAN_TABLE', 'my_query'));
